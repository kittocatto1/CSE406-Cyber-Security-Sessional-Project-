# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/

# ── Lab artifacts (generated during runs — don't commit) ──
*.pcap                 # tcpdump captures (baseline/capture.sh)
baseline_result.txt    # iperf3 baseline output
ss_timeline.log        # ss -ti samples during Attack 2
metrics.csv            # collect_metrics.py output
*.png                  # plot_results.py graphs

# ── Editor / OS ───────────────────────────────────────────
.vscode/
.idea/
.DS_Store
